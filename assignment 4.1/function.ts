//namespace Ns 
namespace Ns {
    //exported function to access and consume in the bundle file
    export const addition = (a: number, b: number): string => (`Addition of ${a} + ${b} is ${a + b}`);
}

